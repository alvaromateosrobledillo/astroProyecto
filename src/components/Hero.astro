---
const backgroundImage = "/fondo_hero.webp"; // Ajusta la ruta de la imagen según corresponda
---

<section id="inicio" class="relative h-screen bg-cover bg-center" style={`background-image: url(${backgroundImage});`}>
  <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-transparent to-black/70 flex flex-col justify-center items-center text-center text-white p-4">
    <h1 class="fade-in text-5xl md:text-6xl font-extrabold mb-6 drop-shadow-lg tracking-wide">LA MARTINA</h1>
    <p class="fade-in text-2xl md:text-3xl mb-8 bg-black bg-opacity-75 p-6 rounded-md shadow-lg font-semibold transform hover:scale-105 transition-transform duration-300">
      EMPRESA DE INGENIERÍA E INSTALADORA. PERSONAL PROPIO.
    </p>
    <p class="fade-in text-xl md:text-2xl mb-8 bg-black bg-opacity-50 p-4 rounded-md shadow-lg font-medium italic">
      EMPRESA JOVEN, DINÁMICA, CON EXPERIENCIA EN EL SECTOR RENOVABLE Y ELECTRICIDAD.
    </p>
    <a href="#contacto" class="fade-in bg-accent text-white py-4 px-10 rounded-full text-xl font-semibold hover:bg-amber-600 transition-transform transform hover:scale-105 duration-300 pulsate">SOLICITA ESTUDIO</a>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const elements = document.querySelectorAll(".fade-in");
    elements.forEach((element, index) => {
      setTimeout(() => {
        element.classList.add("visible");
      }, index * 500); // Delay entre las animaciones de cada elemento
    });
  });
</script>

<style>
  .fade-in {
    opacity: 0;
    transition: opacity 1s ease-in-out, transform 1s ease-in-out;
    transform: translateY(20px);
  }
  .fade-in.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .pulsate {
    animation: pulsate 2s infinite;
  }
  @keyframes pulsate {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
  }
</style>
